import './style.scss';



const Projetos = ({children}) => {
    return (
            <section id="projetos" className="projetos">
                {children}
            </section>
    )
}

export default Projetos;